---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/helmrelease-helm-v2beta1.json
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: &app nut-exporter-ups1
  namespace: monitoring
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 2.2.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 2
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    controllers:
      main:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          main:
            image:
              repository: boardyuk/nut-influxdb-exporter
              tag: latest # has no other tag, heh
            env:
              HOSTNAME: &upsname ups1
              INFLUXDB_DATABASE: nutups
              INFLUXDB_HOST: influxdb
              INFLUXDB_PORT: "8086"
              NUT_HOST: ${SECRET_UPS1_NUT_IP}
              NUT_PORT: "3493"
              UPS_NAME: *upsname
              WATTS: "900"
              INTERVAL: "15"
            envFrom:
              - secretRef:
                  name: nut-explorer-ups1-secret